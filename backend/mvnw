#!/bin/sh
set -e

# Minimal Maven wrapper shim (no download). Requires `mvn` installed.
if command -v mvn >/dev/null 2>&1; then
  exec mvn "$@"
fi

echo "Maven is not installed or not on PATH. Please install Maven (or add it to PATH) and retry." 1>&2
exit 1

