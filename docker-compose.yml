# Docker is optional and not required for local (non-Docker) workflows.
# TODO: add backend service definition (ports, env, volumes) if team opts into Docker.
# TODO: keep this file comments-only until Docker is needed to avoid workflow coupling.

